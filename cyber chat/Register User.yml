info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: "http://localhost:3232/users "
  body:
    type: json
    data: |-
      {
          "email": "hacker@domain.com",
          "password": "super-secret-password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
